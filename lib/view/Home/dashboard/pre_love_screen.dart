import 'package:flutter/material.dart';

class PreLoveScreen extends StatelessWidget {
  const PreLoveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PreLoveScreen...'),
    );
  }
}